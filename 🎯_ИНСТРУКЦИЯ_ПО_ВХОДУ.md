# 🎯 КАК ВОЙТИ В ПРИЛОЖЕНИЕ

## 🌐 Откройте в Браузере

👉 **http://localhost:3000**

---

## 🔐 Данные для Входа

```
Email:    testuser@demo.com
Password: pass123
```

**Или создайте новый аккаунт** через кнопку "Регистрация"

---

## 📍 Что Доступно

### ✅ Backend API
- **URL**: http://localhost:3001
- **Документация**: Все API endpoints работают
- **База данных**: SQLite (`backend/dev.db`)

### ✅ Frontend Web App
- **URL**: http://localhost:3000 👈 **ГЛАВНАЯ СТРАНИЦА**
- **Страницы**:
  - `/` - Главная панель
  - `/login` - Вход
  - `/register` - Регистрация
  - `/habits` - Привычки
  - `/diary` - Дневник
  - `/beauty` - Красота
  - `/goals` - Цели
  - `/profile` - Профиль

### ✅ Telegram Bot
- **Статус**: Запущен локально
- **Команды**: `/start`, `/menu`, `/habits`, `/diary`, `/goals`

---

## 🧪 Тестовые Аккаунты

| Email | Password |
|-------|----------|
| testuser@demo.com | pass123 |
| arina@example.com | securepass123 |

---

## 📋 Проверка Логов

### Backend
```bash
tail -f /tmp/backend.log
```

### Frontend
```bash
tail -f /tmp/frontend.log
```

### Bot
```bash
tail -f /tmp/bot.log
```

---

## 🛑 Остановка Сервисов

```bash
# Остановить всё
pkill -f "node src/server.js"
pkill -f "node src/bot.js"
pkill -f "next dev"
```

---

## 🔄 Перезапуск

### Backend
```bash
cd /Users/zalogudachi/Desktop/Аринка/backend
npm start
```

### Frontend
```bash
cd /Users/zalogudachi/Desktop/Аринка/frontend
npm run dev
```

### Bot
```bash
cd /Users/zalogudachi/Desktop/Аринка/bot
npm start
```

---

## 🐛 Если Не Работает

### 1. Проверьте процессы
```bash
ps aux | grep node
```

### 2. Проверьте порты
```bash
lsof -i :3000  # Frontend
lsof -i :3001  # Backend
```

### 3. Проверьте логи
```bash
tail -50 /tmp/backend.log
tail -50 /tmp/frontend.log
```

---

## 💡 Полезные Команды

### Просмотр базы данных
```bash
cd backend
npx prisma studio
# Откроется http://localhost:5555
```

### Создание нового пользователя через API
```bash
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"name":"Новый Пользователь","email":"new@example.com","password":"newpass123"}'
```

### Тест входа
```bash
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"testuser@demo.com","password":"pass123"}'
```

---

**🎉 Всё готово к использованию!**

Откройте http://localhost:3000 и войдите с данными `testuser@demo.com` / `pass123`
