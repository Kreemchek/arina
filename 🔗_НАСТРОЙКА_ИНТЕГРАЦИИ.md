# 🔗 Настройка Интеграции После Деплоя Backend

## ✅ Backend Запущен на Railway!

Ваш Backend работает на: **http://localhost:8080** (внутри контейнера Railway)

Теперь нужно получить **публичный URL** и подключить Frontend и Bot.

---

## 📋 Шаг 1: Получить Публичный URL Backend

### В Railway Dashboard:

1. Откройте ваш проект на Railway
2. Перейдите в **Settings** → **Networking**
3. Нажмите **"Generate Domain"** или найдите уже созданный домен
4. Скопируйте URL (пример: `https://arina-production.up.railway.app`)

**Или** в разделе **Deployments** → последний деплой → там будет показан URL

---

## 📋 Шаг 2: Обновить Frontend на Vercel

### Вариант A: Через Vercel Dashboard

1. Откройте https://vercel.com/dashboard
2. Найдите проект **arina**
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите или создайте переменную:
   ```
   Name:  NEXT_PUBLIC_API_URL
   Value: https://ваш-railway-url.up.railway.app/api
   ```
5. Выберите **Production**, **Preview**, **Development**
6. Нажмите **Save**
7. Перейдите в **Deployments**
8. Нажмите **"..."** на последнем деплое → **Redeploy**

### Вариант B: Через Vercel CLI

```bash
cd /Users/zalogudachi/Desktop/Аринка/frontend

# Удалить старую переменную
vercel env rm NEXT_PUBLIC_API_URL

# Добавить новую
vercel env add NEXT_PUBLIC_API_URL
# Введите: https://ваш-railway-url.up.railway.app/api
# Выберите: Production, Preview, Development

# Пересобрать
vercel --prod
```

---

## 📋 Шаг 3: Обновить Bot (Локально)

### Обновить bot/.env:

```bash
cat > /Users/zalogudachi/Desktop/Аринка/bot/.env << 'EOF'
TELEGRAM_BOT_TOKEN="8232747700:AAGIphSHtyMAv7xdQTnmq927YCqNoofB4fY"
API_URL="https://ваш-railway-url.up.railway.app/api"
NODE_ENV=development
EOF
```

**Замените** `ваш-railway-url.up.railway.app` на реальный URL из Railway!

### Перезапустить Bot:

```bash
pkill -f "node src/bot.js"
cd /Users/zalogudachi/Desktop/Аринка/bot
npm start
```

---

## 📋 Шаг 4: Настроить CORS в Backend

### Обновить backend/src/server.js:

Найдите строку с CORS и обновите:

```javascript
app.use(cors({
  origin: [
    'https://ваш-vercel-app.vercel.app',  // Frontend на Vercel
    'http://localhost:3000',               // Локальная разработка
  ],
  credentials: true,
}));
```

**Или** через Environment Variable в Railway:
```
CORS_ORIGIN=https://ваш-vercel-app.vercel.app,http://localhost:3000
```

И обновите код:
```javascript
app.use(cors({
  origin: process.env.CORS_ORIGIN?.split(',') || ['http://localhost:3000'],
  credentials: true,
}));
```

---

## 📋 Шаг 5: Выполнить Миграции БД

### В Railway:

1. Откройте **Deployments** → последний деплой
2. Нажмите **"View Logs"** → **"Run Command"**
3. Выполните:
   ```bash
   npx prisma db push
   ```

**Или** через Railway CLI:
```bash
railway run npx prisma db push
```

---

## ✅ Проверка

### 1. Проверить Backend API:
```bash
curl https://ваш-railway-url.up.railway.app/api/auth/register \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"name":"Test","email":"test@test.com","password":"test123"}'
```

Должен вернуть JWT токен ✅

### 2. Проверить Frontend:
- Откройте ваш Vercel URL
- Попробуйте зарегистрироваться
- Должно работать! ✅

### 3. Проверить Bot:
- Откройте Telegram
- Отправьте боту `/start`
- Попробуйте команды
- Должно работать! ✅

---

## 🎯 Итоговая Схема

```
Frontend (Vercel)
    ↓ HTTPS
    https://ваш-vercel-app.vercel.app
    ↓
Backend (Railway)
    ↓ HTTPS
    https://ваш-railway-url.up.railway.app
    ↓
PostgreSQL (Railway)
    ↓
    База данных

Bot (Локально)
    ↓ HTTPS
    https://ваш-railway-url.up.railway.app
    ↓
Backend (Railway)
```

---

## 🔧 Если Что-то Не Работает

### Backend не отвечает
1. Проверьте логи в Railway
2. Проверьте что миграции выполнены
3. Проверьте DATABASE_URL

### Frontend не может подключиться
1. Проверьте CORS настройки
2. Проверьте NEXT_PUBLIC_API_URL в Vercel
3. Проверьте что URL правильный (с `/api` в конце)

### Bot не работает
1. Проверьте API_URL в bot/.env
2. Проверьте что backend доступен
3. Проверьте логи: `tail -f /tmp/bot.log`

---

## 📝 Быстрая Команда

Создайте файл с вашими URL:

```bash
cat > /Users/zalogudachi/Desktop/Аринка/URLS.txt << 'EOF'
Backend URL:  https://ваш-railway-url.up.railway.app
Frontend URL: https://ваш-vercel-app.vercel.app
EOF
```

---

**Готово! Теперь всё должно работать вместе! 🎉**
