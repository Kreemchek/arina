# 💕 Личный Ассистент Современной Девушки

SaaS MVP веб-приложение с Telegram-ботом для отслеживания привычек, ведения дневника, управления целями и задачами красоты/стиля.

## 🌟 Возможности

### Веб-приложение
- ✨ **Трекер привычек** - отслеживание ежедневных привычек с визуализацией прогресса
- 📔 **Дневник настроения** - ведение личного дневника с оценкой эмоционального состояния
- 💅 **Красота & Стиль** - планирование задач по уходу за собой
- 🎯 **Управление целями** - постановка и отслеживание жизненных целей с разбивкой на шаги
- 📊 **Аналитика и статистика** - визуализация прогресса и достижений
- 📄 **PDF-отчёты** - экспорт прогресса в PDF формат
- 💳 **Заглушка премиум-подписки** - готовая структура для будущей монетизации

### Telegram-бот
- 🔔 **Ежедневные напоминания** (9:00) - мотивация и список задач на день
- 🌙 **Вечерние напоминания** (21:00) - запись настроения
- ✅ **Быстрое выполнение привычек** через inline-кнопки
- 📊 **Просмотр статистики** прямо в Telegram
- 💬 **Интерактивные команды** для управления задачами

## 🏗️ Архитектура проекта

```
Аринка/
├── backend/              # Node.js + Express + Prisma + PostgreSQL
│   ├── src/
│   │   ├── controllers/  # Бизнес-логика
│   │   ├── routes/       # API маршруты
│   │   ├── middleware/   # Аутентификация и валидация
│   │   ├── utils/        # Вспомогательные функции
│   │   └── server.js     # Точка входа
│   └── prisma/
│       └── schema.prisma # Схема базы данных
├── frontend/             # Next.js + React + Tailwind CSS
│   ├── components/       # React компоненты
│   ├── pages/           # Страницы приложения
│   ├── utils/           # API клиент и утилиты
│   └── styles/          # Стили
├── bot/                 # Telegram-бот (Telegraf)
│   └── src/
│       ├── bot.js       # Основной файл бота
│       ├── handlers.js  # Обработчики команд
│       ├── notifications.js # Система уведомлений
│       └── api.js       # Клиент для backend API
├── docker-compose.yml   # PostgreSQL в Docker
└── README.md           # Эта инструкция
```

## 🛠️ Технологический стек

### Backend
- **Node.js** + **Express** - REST API сервер
- **Prisma ORM** - работа с базой данных
- **PostgreSQL** - реляционная база данных
- **bcryptjs** - хеширование паролей
- **jsonwebtoken** - JWT аутентификация

### Frontend
- **Next.js** - React framework с SSR
- **React** - UI библиотека
- **Tailwind CSS** - утилитарный CSS фреймворк
- **Axios** - HTTP клиент
- **jsPDF** - генерация PDF отчётов
- **date-fns** - работа с датами

### Bot
- **Telegraf** - фреймворк для Telegram-ботов
- **node-cron** - планировщик задач для напоминаний
- **Axios** - интеграция с backend API

### Database
- **PostgreSQL 15** - основная БД
- **Docker** - контейнеризация БД

## 📋 Требования

- **Node.js** >= 18.x
- **npm** >= 9.x
- **Docker** и **Docker Compose** (для PostgreSQL)
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/botfather))

## 🚀 Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
# Перейти в директорию проекта
cd Аринка

# Установить зависимости для всех модулей
npm run install:all

# Или установить отдельно:
cd backend && npm install
cd ../frontend && npm install
cd ../bot && npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта (скопируйте из `.env.example`):

```bash
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/assistant_db"

# Backend
PORT=3001
NODE_ENV=development
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Telegram Bot
TELEGRAM_BOT_TOKEN="your-telegram-bot-token-from-botfather"
API_URL="http://localhost:3001/api"

# Premium (заглушка)
PREMIUM_ENABLED=false
```

### 3. Запуск базы данных

```bash
# Запустить PostgreSQL в Docker
docker-compose up -d

# Проверить, что БД запустилась
docker ps
```

### 4. Настройка базы данных (Prisma)

```bash
cd backend

# Сгенерировать Prisma Client
npm run prisma:generate

# Выполнить миграции (создать таблицы)
npm run prisma:migrate

# Опционально: открыть Prisma Studio для просмотра БД
npm run prisma:studio
```

### 5. Запуск всех сервисов

#### Вариант 1: Запустить всё одной командой (рекомендуется для разработки)

```bash
# Из корневой директории
npm run dev
```

Это запустит одновременно:
- Backend API (http://localhost:3001)
- Frontend (http://localhost:3000)
- Telegram Bot

#### Вариант 2: Запустить сервисы отдельно

В **трёх разных терминалах**:

```bash
# Терминал 1: Backend
cd backend
npm run dev

# Терминал 2: Frontend
cd frontend
npm run dev

# Терминал 3: Bot
cd bot
npm run dev
```

### 6. Первый запуск

1. Откройте браузер: http://localhost:3000
2. Зарегистрируйте новый аккаунт
3. Войдите в систему
4. Создайте привычки, цели, записи в дневнике

### 7. Настройка Telegram-бота

1. Получите токен бота у [@BotFather](https://t.me/botfather):
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен

2. Добавьте токен в `.env`:
   ```
   TELEGRAM_BOT_TOKEN="YOUR_BOT_TOKEN_HERE"
   ```

3. Перезапустите бот:
   ```bash
   cd bot
   npm run dev
   ```

4. Найдите бота в Telegram и отправьте `/start`

**⚠️ Важно:** Для связки Telegram-бота с веб-аккаунтом сейчас используется упрощённая схема (заглушка). В продакшене нужно реализовать OAuth или специальный механизм связки через уникальный код.

## 📱 Использование

### Веб-приложение

После запуска откройте http://localhost:3000

**Основные разделы:**
- **Главная** - дашборд с общей статистикой и быстрым доступом
- **Привычки** - создание и отслеживание привычек
- **Дневник** - записи о настроении и мыслях
- **Красота** - задачи по уходу за собой
- **Цели** - долгосрочные цели с разбивкой на шаги
- **Профиль** - статистика, настройки, экспорт PDF

### Telegram-бот

**Основные команды:**
- `/start` - начать работу с ботом
- `/help` - справка по командам
- `/habits` - список привычек с кнопками выполнения
- `/goals` - список целей
- `/mood` - записать настроение
- `/today` - задачи на сегодня
- `/stats` - статистика

**Автоматические уведомления:**
- 🌅 **9:00** - утреннее напоминание о привычках
- 🌙 **21:00** - вечернее напоминание записать настроение

## 🎨 Дизайн

Приложение использует **mobile-first** подход с пастельными цветами:
- Розовый (#FFB6C1)
- Лавандовый (#E6E6FA)
- Персиковый (#FFDAB9)
- Мятный (#C1E1C1)
- Голубой (#AEC6CF)
- Сиреневый (#D8BFD8)

## 📊 API Эндпоинты

### Аутентификация
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход
- `GET /api/auth/profile` - получить профиль
- `POST /api/auth/logout` - выход

### Привычки
- `GET /api/habits` - все привычки
- `POST /api/habits` - создать привычку
- `PATCH /api/habits/:id` - обновить привычку
- `DELETE /api/habits/:id` - удалить привычку
- `POST /api/habits/:id/complete` - отметить выполнение

### Дневник
- `GET /api/diary` - все записи
- `POST /api/diary` - создать запись
- `GET /api/diary/:id` - получить запись
- `PATCH /api/diary/:id` - обновить запись

### Красота
- `GET /api/beauty` - все задачи
- `POST /api/beauty` - создать задачу
- `PATCH /api/beauty/:id` - обновить задачу
- `DELETE /api/beauty/:id` - удалить задачу

### Цели
- `GET /api/goals` - все цели
- `POST /api/goals` - создать цель
- `PATCH /api/goals/:id` - обновить цель
- `DELETE /api/goals/:id` - удалить цель

### Прогресс
- `GET /api/progress` - получить прогресс
- `POST /api/progress` - сохранить прогресс
- `GET /api/progress/stats` - статистика

### Платежи (заглушка)
- `GET /api/payments/status` - статус подписки
- `POST /api/payments/create` - создать платёж

## 🔐 Безопасность

- Пароли хешируются с помощью **bcryptjs**
- Аутентификация через **JWT токены**
- Все приватные маршруты защищены middleware
- CORS настроен для безопасности
- Валидация входных данных с **express-validator**

## 🚧 Заглушки для будущего развития

### Премиум-подписка
- Готова структура для интеграции платёжной системы (Stripe/PayPal)
- API эндпоинты для создания платежей
- UI для апгрейда до премиум
- Проверка премиум-статуса в middleware

### Дополнительные функции
- Социальная авторизация (Google, Apple)
- Уведомления в браузере (Push API)
- Синхронизация между устройствами
- Sharing прогресса в соц. сетях
- Темы оформления
- Мультиязычность

## 🐛 Отладка

### Проблемы с базой данных
```bash
# Остановить и удалить контейнер
docker-compose down

# Удалить данные и запустить заново
docker-compose down -v
docker-compose up -d

# Пересоздать миграции
cd backend
rm -rf prisma/migrations
npm run prisma:migrate
```

### Проблемы с Prisma
```bash
cd backend
rm -rf node_modules/.prisma
npm run prisma:generate
```

### Логи бота
```bash
cd bot
npm run dev
# Логи будут в консоли
```

## 📝 Разработка

### Добавление новой модели в БД
1. Отредактируйте `backend/prisma/schema.prisma`
2. Запустите `npm run prisma:migrate`
3. Создайте контроллер в `backend/src/controllers/`
4. Создайте маршрут в `backend/src/routes/`
5. Подключите маршрут в `backend/src/server.js`

### Создание новой страницы
1. Создайте файл в `frontend/pages/`
2. Добавьте маршрут в навигацию (`frontend/components/Navigation.js`)
3. Используйте Layout компонент для единообразия

### Добавление команды в бота
1. Создайте обработчик в `bot/src/handlers.js`
2. Зарегистрируйте команду в `bot/src/bot.js`

## 🔄 Production готовность

Для деплоя в продакшен необходимо:

1. **Backend:**
   - Использовать production БД (не Docker локально)
   - Настроить HTTPS
   - Включить rate limiting
   - Настроить логирование (Winston, Pino)
   - Добавить мониторинг (Sentry)

2. **Frontend:**
   - Собрать production build: `npm run build`
   - Деплоить на Vercel/Netlify/AWS
   - Настроить CDN для статики

3. **Bot:**
   - Использовать webhook вместо polling
   - Деплоить на VPS/Heroku
   - Настроить PM2 для автозапуска

4. **Database:**
   - Использовать managed PostgreSQL (AWS RDS, Supabase, Railway)
   - Настроить backup
   - Включить SSL connection

5. **Security:**
   - Изменить все секретные ключи
   - Настроить CORS properly
   - Добавить rate limiting
   - Настроить CSP headers

## 📄 Лицензия

MIT

## 👥 Контакты

Создано для демонстрации MVP SaaS приложения.

---

**Приятного использования! 💕✨**

Если у вас есть вопросы или предложения, создайте issue в репозитории.

