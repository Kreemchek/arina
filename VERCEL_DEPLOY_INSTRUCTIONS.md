# 🚀 Деплой Frontend на Vercel

## ⚡ Быстрый деплой (3 минуты)

### Вариант 1: Через Vercel Dashboard (рекомендуется)

1. **Перейдите на Vercel:**
   - Откройте https://vercel.com
   - Войдите или зарегистрируйтесь (можно через GitHub)

2. **Создайте новый проект:**
   - Нажмите **"Add New..."** → **"Project"**
   - Выберите **"Import Git Repository"**
   
3. **Импортируйте проект:**
   - **Опция A**: Если проект в GitHub
     - Выберите ваш репозиторий
     - Нажмите **"Import"**
   
   - **Опция B**: Если проект локально
     - Установите Vercel CLI (см. ниже)

4. **Настройте проект:**
   - **Framework Preset**: Next.js (должно определиться автоматически)
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build` (автоматически)
   - **Output Directory**: `.next` (автоматически)
   - **Install Command**: `npm install` (автоматически)

5. **Добавьте Environment Variables:**
   Нажмите **"Environment Variables"** и добавьте:
   ```
   Name: NEXT_PUBLIC_API_URL
   Value: http://localhost:3001/api
   ```
   
   ⚠️ **ВАЖНО**: После деплоя backend, вернитесь сюда и измените на реальный URL!

6. **Deploy:**
   - Нажмите **"Deploy"**
   - Подождите 2-3 минуты
   - Готово! 🎉

7. **Получите URL:**
   - Vercel даст вам URL типа: `https://your-app.vercel.app`
   - Сохраните этот URL!

---

### Вариант 2: Через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm install -g vercel
   ```

2. **Войдите:**
   ```bash
   vercel login
   ```

3. **Деплой:**
   ```bash
   cd frontend
   vercel
   ```

4. **Следуйте инструкциям:**
   - Set up and deploy? **Y**
   - Which scope? *выберите ваш аккаунт*
   - Link to existing project? **N**
   - What's your project's name? `personal-assistant` (или любое)
   - In which directory is your code located? `./` (текущая)
   - Want to override settings? **N**

5. **Добавьте Environment Variable:**
   ```bash
   vercel env add NEXT_PUBLIC_API_URL
   # Введите: http://localhost:3001/api
   # Выберите: Production, Preview, Development (все)
   ```

6. **Готово!**
   - URL будет показан в терминале
   - Также доступен в Dashboard: https://vercel.com/dashboard

---

## 🔄 Обновление после деплоя Backend

Когда вы задеплоите backend (позже), обновите переменную:

### Через Dashboard:
1. Откройте проект на Vercel
2. **Settings** → **Environment Variables**
3. Найдите `NEXT_PUBLIC_API_URL`
4. Измените на: `https://your-backend-url.com/api`
5. **Save**
6. Перейдите в **Deployments**
7. Нажмите **"Redeploy"** на последнем деплое

### Через CLI:
```bash
vercel env rm NEXT_PUBLIC_API_URL
vercel env add NEXT_PUBLIC_API_URL
# Введите новый URL
vercel --prod
```

---

## ✅ Проверка

1. Откройте ваш URL: `https://your-app.vercel.app`
2. Попробуйте зарегистрироваться
3. ⚠️ **Ожидаемо**: Ошибка подключения к API (backend пока локальный)

**Это нормально!** Backend работает локально и недоступен из интернета.

---

## 🔧 Следующие шаги (опционально)

Если хотите полностью рабочее приложение в интернете:

1. **Деплой Backend** (выберите один):
   - Railway (бесплатный план)
   - Render (бесплатный план)
   - Heroku (платный)

2. **Обновите NEXT_PUBLIC_API_URL** на Vercel

3. **Готово!** Приложение работает полностью онлайн

---

## 🎯 Текущая схема работы

```
Internet
   ↓
Vercel Frontend (онлайн)
   ↓ ❌
Backend (локально) ← не доступен из интернета
```

После деплоя backend:
```
Internet
   ↓
Vercel Frontend (онлайн)
   ↓ ✅
Backend на Railway/Render (онлайн)
   ↓
Database на Railway/Render (онлайн)
```

---

## 📝 Заметки

- Frontend деплоится мгновенно (2-3 минуты)
- Автоматический CI/CD при push в git
- Бесплатный SSL сертификат
- Автоматические превью для PR
- CDN по всему миру

---

**Frontend готов! Теперь можно работать с ботом локально.** 🚀

